import { StyleSheet } from "react-native";

const textStyles = StyleSheet.create({
  heading: {
    color: "#000",
    fontSize: 25,
    fontWeight: "700",
    textAlign: "center",
  },
  bold: {
    fontWeight: "700",
  },
  normal: {
    fontWeight: "400",
  },
});
export default textStyles;
